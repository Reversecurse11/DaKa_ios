# iOS 全页面玻璃化重设计 Design QA

## 结论

**PASSED**

首页、通知、课程、运动打卡、成绩和“我的”页面已经完成统一的原生 iOS 玻璃化重设计。最终版本未发现 P0、P1 或 P2 级视觉与交互问题，可交由产品侧真机检查。

## 验证基线

- 设计真值：`DesignReferences/home-glass-selected.png`
- 首页最终实现：`DesignReferences/home-glass-implementation-final-zh.png`
- 首页全屏对照：`DesignReferences/home-glass-comparison-all-final.png`
- 首页重点区域对照：`DesignReferences/home-glass-comparison-all-final-focus.png`
- 中文全页面总览：`DesignReferences/glass-all-screens-final-zh.png`
- 英文页面证据：`DesignReferences/visual-regression-native-titles/`
- 设计图尺寸：852 × 1846
- 实现截图尺寸：1206 × 2622
- 对照尺寸：设计图 852 × 1846；实现图等比缩放至 850 × 1846；并排图 1702 × 1846
- 运行设备：iPhone 17 模拟器，iOS 26.5，402 × 874 pt，3×
- 状态：测试账号已登录；总进度 16/20 小时；课程相关 6/10 小时；其他运动 10/10 小时；组织认证抵扣 +10 小时；浅色模式

对照图左侧为设计真值，右侧为真实 SwiftUI 运行截图。实现图中的系统状态栏、灵动岛、原生导航栏和 Tab Bar 属于真实 iOS 运行环境，不作为设计偏差。

## 页面核对

### 首页

- 保留“剩余学时 → 优先事项 → 本学期进度 → 主操作”的单一路径。
- 删除高密度风险面板、重复记录和多色状态块。
- 使用原校徽、BNBU 蓝、系统字体、SF Symbols 与原生 Material。
- 底部操作区使用安全区域，不与系统 Tab Bar 重叠。
- “开始运动”进入提交分段，“查看记录”进入记录分段。

### 通知

- 数字徽标完整显示在工具栏内部，不再裁切或越界。
- 四个筛选为可横向滚动的胶囊按钮，英文状态下不压缩、不截断。
- 未读筛选中打开详情不会先移除当前消息或打断导航。
- 已读状态通过详情页显式操作修改；“全部已读”继续可用。
- 列表保留分类、时间、未读状态和详情入口。

### 课程

- 使用原生大标题和低密度单列玻璃卡片。
- 保留课程代码、Section、名称、教师、学年、学期、状态和详情。
- 当前课程与历史课程继续读取原有接口数据。

### 运动打卡

- 使用原生大标题和玻璃分段选择器。
- 保留提交、记录、GPS、计时、暂停、结束确认、草稿、凭证和提交逻辑。
- 首页两个入口可稳定打开指定分段。

### 成绩

- 四个成绩板块改为纵向玻璃列表，中英文采用同一对齐规则。
- 总分预估独立突出，缺失项继续可见。
- 计算公式和来源收纳至“查看计算说明”。
- 页面滚动时使用原生折叠导航标题，不与状态栏重叠。

### 我的

- 学生资料、申请、教师、组织认证、外观、语言和帮助统一为玻璃分组。
- 详细学生资料改为可展开区域，降低首屏密度。
- 帮助、新手引导、隐私和退出登录使用紧凑设置行。
- 退出登录的实际点击区域已扩大，确认和状态清理逻辑保持不变。

## 视觉与适配

- 字体：系统 SF / 中文系统字体；大标题使用原生导航栏折叠行为。
- 间距：卡片、行、分组与安全区域使用统一节奏。
- 色彩：以 BNBU 蓝和中性白灰为主，仅业务状态使用必要的语义色。
- 资产：保留项目原始 `bnbu_emblem` 和 SF Symbols，不使用近似绘制或占位图标。
- Material：卡片、通知入口、底部操作区、导航栏和 Tab Bar 使用原生材质与细描边。
- 语言：已验证 `zh-Hans` 与 `en`；客户端 UI 文案完整本地化。
- 数据原文：课程名称、教师、通知正文等后端录入内容按业务文档要求不进行客户端伪翻译。
- 可访问性：主要页面、按钮、筛选项、通知和进度组件保留稳定的可访问性标识与语义标签。

## 迭代记录

1. 首页初版
   - P1：大标题造成顶部空白，真实进度值破坏行内对齐。
   - 修正：内容区内联首页标题；进度改为紧凑、自适应结构。

2. 首页第二轮
   - P2：优先提示重复、分区标题不统一、主次按钮比例失衡。
   - 修正：精简提示、统一标题、收敛按钮比例。

3. 全页面第一轮
   - P2：通知数字徽标被裁切；未读筛选打开详情时存在列表与导航竞争。
   - P2：英文成绩四方框对齐不稳定。
   - 修正：徽标定位至工具栏内部；仅在详情显式标记已读；成绩改为纵向列表。

4. 全页面第二轮
   - P1：长页面滚动后自定义标题可能与状态栏重叠。
   - P2：英文通知筛选和计算说明标签可能压缩换行。
   - 修正：课程、打卡、成绩和“我的”统一使用原生大标题；通知筛选横向滚动；计算说明移除冗余标签。

5. 最终对照
   - P0：0
   - P1：0
   - P2：0
   - P3：后端 Mock 数据在英文界面仍保留中文原文，符合业务文档的数据归属规则。

## 自动化验证

- Xcode 最终串行全量测试：**100 项**
- 通过：**97**
- 失败：**0**
- 跳过：**3**（远程环境/凭据门禁测试，按预期跳过）
- 预期失败：**0**
- 覆盖：单元测试、登录/退出、首页、通知筛选与详情、课程、打卡、成绩、我的、中英文布局和视觉回归
- 通知与退出登录易波动流程额外连续运行 3 轮，全部通过
- 徽标安全区最终调整后，通知未读筛选与详情专项 UI 测试 1/1 通过
- `Localizable.xcstrings` JSON 校验通过
- `git diff --check` 通过

final result: passed
