# iOS 全页面玻璃化 UI 重设计日志

日期：2026-07-25

分支：`agent/ios-glass-redesign-20260725`

状态：完成，发布至 GitHub 功能分支

## 本次目标

- 以已确认的第三版首页效果图为视觉基线。
- 保留 BNBU 蓝、原校徽、原 SF Symbols 图标和全部数据/API 接口。
- 减少信息密度和杂色，统一为原生 iOS 大标题、Material 玻璃卡片和浮动 Tab Bar。
- 保留业务流程中的课程、运动打卡、成绩、免测/抵扣、通知、设置和中英文能力。

## 页面修改

### 首页

- 重构为“剩余学时 → 优先事项 → 本学期进度 → 主操作”单一路径。
- 删除首页高密度风险、重复记录和多色状态区。
- 底部固定“开始运动 / 查看记录”玻璃操作栏。
- “开始运动”进入提交分段，“查看记录”直接进入记录分段。
- 继续使用真实 `AppState` 数据和组织认证抵扣值。

### 通知

- 修复通知数字徽标越界、被工具栏裁切的问题。
- 修复在“未读”筛选中点开通知时，消息先被移除并可能打断详情跳转的问题。
- 打开通知不再自动改为已读；在详情页显式标记，避免列表数据与导航栈竞争。
- 四个筛选改为可横向滚动的胶囊按钮，英文不会压缩或截断。
- 通知列表改为低密度玻璃行，保留分类、时间、未读状态和详情入口。
- 新增未读筛选与详情跳转稳定性 UI 测试。

### 课程

- 使用原生 iOS 大标题。
- 课程卡片由四宫格信息改为单列玻璃卡片。
- 保留课程代码、Section、课程名称、老师、学年、学期和修读状态。
- 当前课程与历史课程继续使用原数据和详情入口。

### 运动打卡

- 使用原生大标题与玻璃分段选择器。
- 所有既有计时、暂停、继续、结束确认、GPS、草稿、凭证和提交逻辑保持不变。
- 首页入口可以明确切换至“提交”或“记录”分段。
- 表单、草稿和记录卡片继承统一玻璃组件。

### 成绩

- 四个成绩板块由 2×2 方框改为一个稳定的纵向玻璃列表。
- 中英文使用相同对齐规则，长英文标题不会破坏方框高度。
- 总分预估作为单独重点卡片。
- 缺失项继续可见；计算公式和来源追溯收纳到可展开的“查看计算说明”。
- 滚动时使用原生折叠导航标题，避免内容与状态栏重叠。

### 我的

- 使用原生 iOS 大标题。
- 学生资料、申请、老师、组织认证、外观、语言和帮助功能统一为玻璃分组。
- 详细学生资料改为可展开区域，首屏信息密度降低。
- 帮助、新手引导、隐私和退出登录改为紧凑设置行。
- 扩大退出登录的实际点击区域；确认和清理逻辑保持不变。

## 公共组件

- `BNBUPageBackground`：改为低对比度白灰背景。
- `SwissPanel`：统一使用 `.ultraThinMaterial`、细描边和轻阴影。
- `BrandMark`：原校徽改为圆形玻璃承载。
- `StatusBadge`：统一为胶囊状态。
- 主次按钮：统一为圆角胶囊和 Material 次按钮。
- Tab Bar 与导航栏：继续使用系统 Material 和 BNBU 蓝色强调色。

## 本地化与适配

- 新增并校验课程摘要、成绩构成、计算说明、学生信息等中英文文案。
- 教师、课程名称、通知正文等后端录入内容按业务文档要求保留原文，不进行客户端伪翻译。
- 已生成中文六页面和英文七状态视觉回归截图。
- 英文课程、打卡、成绩和个人页无 UI 文案中文残留；后端 Mock 内容中的中文属于数据原文。

## 测试与证据

- 中文全页面总览：`DesignReferences/glass-all-screens-final-zh.png`
- 英文页面截图：`DesignReferences/visual-regression-native-titles/`
- 首页最终对照：`DesignReferences/home-glass-comparison-all-final.png`
- 首页重点区域对照：`DesignReferences/home-glass-comparison-all-final-focus.png`
- Design QA：`design-qa.md`
- Xcode 最终串行全量测试：100 项，97 通过，0 失败，3 项远程环境/凭据门禁测试按预期跳过。
- 通知与退出登录易波动流程额外连续运行 3 轮，全部通过。
- 通知徽标安全区最终调整后，未读筛选与详情专项 UI 测试 1/1 通过。
- `Localizable.xcstrings` JSON 校验通过。
- `git diff --check` 通过。

## 范围说明

- 未修改后端 API、数据模型或生产环境配置。
