# iOS Mock 测试账号

## 账号信息

- 运行方案：`BNBUStudent Mock`
- 邮箱：`test.student@bnbu.edu.cn`
- 手机号：`13800138000`
- 验证码：`123456`
- 学号：`2400123456`

该账号没有密码。点击登录页的“使用 Mock 用户”会直接进入这个已登录的测试账号，无需填写邮箱、手机号或验证码。邮箱和手机号验证码登录仍作为备用测试路径保留。

## 真机运行

1. 用 Xcode 打开 `BNBUStudent.xcodeproj`。
2. 顶部 Scheme 选择 `BNBUStudent Mock`，设备选择已连接的 iPhone。
3. 点击 Run，完成隐私说明和新手指引。
4. 点击“使用 Mock 用户”即可直接进入完整测试账号界面。若需要单独测试验证码流程，也可选择邮箱或手机号登录，点“填入测试账号”，再依次点“获取验证码”和“登录”。

Mock Scheme 默认带有 `-mock-test-account`，并使用 `-demo-relax-time-window`，方便在 06:00–22:00 之外测试运动流程。如需核验北京时间打卡门禁，在 Scheme 的 Run > Arguments 中临时取消 `-demo-relax-time-window`。

## 快速增加本次运动时长

使用测试账号开始运动后，进行中页面会显示“测试：增加 1 小时”。点击一次会给当前运动计时增加 1 小时，但不会直接修改运动进度或伪造历史记录。随后按正常流程结束运动、选择已拍摄的凭证并提交，才会保存一条本地 Mock 打卡记录并累计对应学时。

## 可测试范围

测试账号可本地完成登录、课程与课程详情、运动开始/暂停/结束、本次运动时长快速增加、相机和麦克风权限、照片和视频凭证、打卡提交与记录详情、通知已读、成绩页、免测申请及补材料、耐力跑换算、联系方式验证、问题反馈、帮助中心、中英文和深浅色切换。

免测、反馈、打卡等写操作会保存到本机 Mock 工作区并生成相应页面状态和通知。退出登录会清理本地测试数据；重新进入测试账号后恢复初始数据。

## 边界

- 不会发送真实短信或邮件。
- 不会上传到真实对象存储，也不会写入后端审核队列。
- 耐力跑结果使用本地固定测试曲线，只用于验证客户端界面和交互，不代表正式评分标准。
- 真实 API、跨设备同步、服务端审核、服务端失败重试和后端全链路仍需 Staging 环境验收。
- 测试账号的具体值只编译进 Debug Fixture；Staging 和 Release 不包含这些账号值。
